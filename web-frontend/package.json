{
  "name": "telegram-webapp",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r web-frontend/* dist/",
    "start": "serve -s . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
